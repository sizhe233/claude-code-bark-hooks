# ============================================
# Claude Code Bark 通知配置文件模板
# 复制为 .env 文件并填入您的实际配置
# ============================================

# ============================================
# Bark 通知基础配置
# ============================================

# Bark推送密钥（必须配置）
BARK_KEY=YOUR_BARK_KEY

# Bark API地址（可选，默认使用官方服务）
BARK_API=https://api.day.app

# 通知分组（可选）
BARK_GROUP=Claude-Code

# 通知声音（可选）
BARK_SOUND=default

# 通知级别（可选：active、timeSensitive、passive）
BARK_LEVEL=active

# ============================================
# Hooks 事件开关配置
# ============================================
# 设置为 true 开启，false 关闭
# 注意：修改后需要重启 Claude Code 才能生效

# 用户提交消息时通知
HOOK_USER_PROMPT=true

# 任务停止/完成时通知  
HOOK_STOP=true

# 工具使用后通知（Edit、Write、Bash等）
HOOK_POST_TOOL_USE=true

# 系统通知（需要用户确认时）
HOOK_NOTIFICATION=true

# 错误发生时通知
HOOK_ERROR=true

# 工具使用前通知（较少使用，默认关闭）
HOOK_PRE_TOOL_USE=false